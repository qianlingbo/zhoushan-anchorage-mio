name: Update Data

on:
  schedule:
    # GitHub cron uses UTC. This runs every 3 hours at 00:37, 03:37, 06:37,
    # 09:37, 12:37, 15:37, 18:37, and 21:37 Asia/Shanghai.
    - cron: "37 1,4,7,10,13,16,19,22 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: anchorage-data-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    env:
      TZ: Asia/Shanghai
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --upgrade pip requests

      - name: Update anchorage data
        run: |
          for attempt in 1 2 3; do
            if python scripts/update_data.py --no-cache-on-failure; then
              exit 0
            fi

            if [ "$attempt" = "3" ]; then
              break
            fi

            echo "Data source unavailable, retrying in 5 minutes..."
            sleep 300
          done

          echo "Data source still unavailable; keeping cached data and recording this check."
          python scripts/update_data.py --cache-only

      - name: Commit and push data changes
        run: |
          if git diff --quiet -- data/latest.json data/data.js; then
            echo "No data changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/latest.json data/data.js
          git commit -m "auto update data"
          git push
