# 舟山锚地供油指数看板 — 运维手册

## 项目概览

一个自动更新的锚地天气看板，展示舟山四个锚地（条帚门、虾峙门外、马峙、秀山东）的 MIO 供油指数精细化预报。

```
┌──────────────┐     ┌───────────────┐     ┌──────────────────┐     ┌────────────────┐
│  舟山海洋气象台  │────▶│  GitHub Actions │────▶│  GitHub 仓库       │────▶│  GitHub Pages    │
│  zs121.com.cn  │     │  定时抓数据      │     │  存代码+数据       │     │  托管网页         │
│  (API数据源)    │     │  定点多次检查    │     │  自动触发部署      │     │  用户访问看板     │
└──────────────┘     └───────────────┘     └──────────────────┘     └────────────────┘
```

## 关键信息

| 项目 | 内容 |
|------|------|
| **GitHub 仓库** | https://github.com/qianlingbo/zhoushan-anchorage-mio |
| **在线看板** | https://mio.zsagent01.com （主地址） |
| **备用地址** | https://www.zsagent01.com （301跳转到 mio） |
| **本地项目目录** | `/Users/qianlingbo/Documents/网站制作` |
| **数据源 API** | `https://www.zs121.com.cn/gh/SubjectiveForecast/groundAnchorageNew?name=锚地名` |
| **域名注册商** | Cloudflare（zsagent01.com） |
| **DNS 托管** | Cloudflare |
| **线上自动更新** | GitHub Actions：围绕北京时间 06:00、12:00、16:00、20:00 检查 |
| **本地备用日志** | `/tmp/mio-update.log` |

## 运行逻辑

### 整体流程

1. **GitHub Actions** 在源站常见发布时间后自动触发多次检查
2. **Python 脚本**（`scripts/update_data.py`）请求舟山海洋气象台 API，抓取四个锚地的预报数据
3. 如果源站预报内容有变化，脚本将数据写入 `data/latest.json` 和 `data/data.js`
4. **GitHub Actions commit + push** 将新数据推送到 GitHub
5. **GitHub Pages** 检测到 main 分支更新，自动重新部署网页
6. 用户访问 `mio.zsagent01.com` 看到最新数据

### 自动更新不需要你做任何操作

GitHub Actions 会在云端自动跑。你什么都不用管。

### 什么情况下不会更新

- GitHub Actions 排队延迟（通常几分钟内恢复）
- 数据源临时不可访问（工作流会重试，仍失败则沿用缓存）
- zs121.com.cn 服务器宕机（少见，等下次自动恢复）

---

## 日常操作

### 查看是否正常运行

```bash
cat /tmp/mio-update.log
```

正常输出长这样：
```
=== 舟山锚地供油指数数据更新 ===
时间: 2026-04-25 10:00:01
抓取 条帚门锚地 ...
  ✓ 成功，24 条时段数据
抓取 虾峙门外锚地 ...
  ✓ 成功，24 条时段数据
...
全部完成！
```

### 手动触发一次更新

```bash
cd ~/Documents/网站制作 && python3 scripts/update_data.py && git add data/ && git commit -m "🔄 手动更新" && git push
```

### 查看定时任务配置

```bash
crontab -l
```

### 修改更新时间

线上更新时间在 `.github/workflows/update.yml` 的 `cron` 配置里修改。

时间格式：`分 时 * * *`
- `15,45 6,12,16,20` = 06:15、06:45、12:15、12:45... 检查
- `30 8` = 每天 08:30
- `0 8,12,17` = 每天 8:00、12:00、17:00（三次）

### 临时停止本地备用更新

```bash
crontab -r
```

### 恢复本地备用更新

找 Cola 帮你重新配，或者手动：
```bash
crontab -e
```
粘贴以下内容保存：
```
# 舟山锚地MIO数据更新 — 围绕 06/12/16/20 点检查
PATH=/usr/local/bin:/usr/bin:/bin
15,45 6,12,16,20 * * * /Users/qianlingbo/Documents/网站制作/scripts/cron-update.sh
```

---

## 项目文件结构

```
~/Documents/网站制作/
├── index.html          ← 看板主页面
├── app.js              ← 渲染逻辑（读取数据、生成表格）
├── styles.css          ← 样式
├── CNAME               ← GitHub Pages 自定义域名配置
├── scripts/
│   └── update_data.py  ← 数据抓取脚本（核心）
└── data/
    ├── latest.json     ← API 原始数据（每次更新覆盖）
    └── data.js         ← 同一份数据的 JS 格式（供 file:// 使用）
```

---

## 域名与 DNS 配置

当前 Cloudflare DNS 记录：

| Type | Name | Content | 说明 |
|------|------|---------|------|
| CNAME | `mio` | `qianlingbo.github.io` | 主访问地址 |
| Page Rule | `www.zsagent01.com/*` | 301 → `mio.zsagent01.com/$1` | www 跳转到 mio |

GitHub Pages 的 custom domain 设置为 `mio.zsagent01.com`。

---

## 故障排查

### 网页显示旧数据

1. 浏览器强制刷新：`Cmd + Shift + R`
2. 检查日志：`cat /tmp/mio-update.log`，看最后一次是否成功
3. 检查 GitHub 是否收到推送：https://github.com/qianlingbo/zhoushan-anchorage-mio/commits/main

### 日志显示抓取失败

```
⚠ 失败: HTTPSConnectionPool ... timed out
```
→ zs121.com.cn 暂时不可达，等下次自动重试即可。如果连续多天失败，可能是对方改了接口。

### git push 失败

```
error: failed to push some refs
```
→ 远程有更新，执行：
```bash
cd ~/Documents/网站制作 && git pull --rebase && git push
```

如果有冲突：
```bash
cd ~/Documents/网站制作 && git stash && git pull && git stash drop && python3 scripts/update_data.py && git add data/ && git commit -m "🔄 手动更新" && git push
```

### 网页完全打不开（404）

1. 检查 GitHub Pages 是否开启：仓库 Settings → Pages → Source 应为 `main` 分支
2. 检查 CNAME 文件是否存在：`cat ~/Documents/网站制作/CNAME`，应该输出 `mio.zsagent01.com`
3. 检查 Cloudflare DNS 的 `mio` CNAME 记录是否指向 `qianlingbo.github.io`

### 定时任务没执行

1. 确认电脑没有在休眠：合盖期间 crontab 不运行
2. 确认 crontab 还在：`crontab -l`
3. macOS 可能会弹窗要求授权 cron 的磁盘访问权限 → 去 系统设置 → 隐私与安全性 → 完全磁盘访问权限，检查 `/usr/sbin/cron` 是否在列表中
